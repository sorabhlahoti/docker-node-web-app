{
  "name": "nodejs_docker_web_app",
  "version": "1.0.0",
  "description": "Nodejs app on docker container",
  "main": "server.js",
  "type":"module",
  "scripts": {
    "start":"nodemon --legacy-watch server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sorabh lahoti",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}